<Page x:Class="Kapal.Views.VesselDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Data Vessel"
      Background="#F8FAFC">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32,24,32,32">
            
   <!-- Header -->
 <TextBlock Text="Data Vessel" FontSize="32" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
    <TextBlock Text="Manage vessel data - Create, Update, Delete" FontSize="14" Foreground="#64748B" Margin="0,0,0,32"/>

       <!-- Content -->
     <Border CornerRadius="12" Background="White" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1">
  <StackPanel>
       <DockPanel Margin="0,0,0,16">
  <Button Click="BtnAdd_Click" DockPanel.Dock="Right" Style="{StaticResource PrimaryButtonStyle}">
         <StackPanel Orientation="Horizontal">
        <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,8,0"/>
           <TextBlock Text="Add Vessel" VerticalAlignment="Center"/>
        </StackPanel>
   </Button>
 <TextBlock Text="Vessel List" FontSize="18" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
        </DockPanel>

    <!-- DataGrid -->
     <DataGrid x:Name="dgVessels" Height="500" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
    SelectionMode="Single" SelectionChanged="DgVessels_SelectionChanged"
 HeadersVisibility="Column" GridLinesVisibility="None" Background="White"
      RowBackground="White" AlternatingRowBackground="#F9FAFB"
       BorderBrush="#E2E8F0" BorderThickness="1">
  <DataGrid.Columns>
    <DataGridTextColumn Header="ID" Binding="{Binding VesselId}" Width="80"/>
      <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
   <DataGridTextColumn Header="Reg Number" Binding="{Binding RegNumber}" Width="140"/>
     <DataGridTextColumn Header="Owner" Binding="{Binding OwnerName}" Width="*"/>
    <DataGridTextColumn Header="Gear" Binding="{Binding Gear}" Width="120"/>
          </DataGrid.Columns>
       </DataGrid>

  <!-- Action Buttons -->
       <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
       <Button x:Name="btnEdit" Click="BtnEdit_Click" IsEnabled="False"
    Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0">
               <StackPanel Orientation="Horizontal">
   <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" Margin="0,0,8,0"/>
              <TextBlock Text="Edit" VerticalAlignment="Center"/>
      </StackPanel>
           </Button>
      <Button x:Name="btnDelete" Click="BtnDelete_Click" IsEnabled="False" Style="{StaticResource DangerButtonStyle}">
    <StackPanel Orientation="Horizontal">
      <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,8,0"/>
           <TextBlock Text="Delete" VerticalAlignment="Center"/>
   </StackPanel>
     </Button>
   </StackPanel>
     </StackPanel>
   </Border>

 </StackPanel>
    </ScrollViewer>
</Page>
