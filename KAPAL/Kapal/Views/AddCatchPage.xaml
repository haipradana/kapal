<Page x:Class="Kapal.Views.AddCatchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Add Catch">
    <Grid Margin="16" RenderTransformOrigin="0.5,0.5" Height="260" Width="557">
        <Grid.RenderTransform>
            <TransformGroup>
                <ScaleTransform/>
                <SkewTransform/>
                <RotateTransform Angle="0.156"/>
                <TranslateTransform/>
            </TransformGroup>
        </Grid.RenderTransform>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- header & form -->
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <!-- list species -->
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <!-- action buttons -->
        </Grid.RowDefinitions>

        <!-- Header + form -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Add Catch" FontSize="18" FontWeight="Bold" Margin="0 0 0 12"/>
            <TextBlock x:Name="lblLanding" Margin="0 0 0 12" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Species" Grid.Row="0" Grid.Column="0" Margin="0 0 8 8" VerticalAlignment="Center"/>
                <TextBox x:Name="tbSpecies" Grid.Row="0" Grid.Column="1" Margin="0 0 8 8" />
                <Button x:Name="btnAddSpecies" Grid.Row="0" Grid.Column="2" Width="120" Height="30"
                        Content="Add Species" Click="BtnAddSpecies_Click"/>

                <TextBlock Text="Weight (kg)" Grid.Row="1" Grid.Column="0" Margin="0 0 8 0" VerticalAlignment="Center"/>
                <TextBox x:Name="tbWeight" Grid.Row="1" Grid.Column="1" />
            </Grid>
        </StackPanel>

        <!-- Daftar species sementara -->
        <DataGrid x:Name="dgSpecies" Grid.Row="2"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding No}" Width="60"/>
                <DataGridTextColumn Header="Species" Binding="{Binding Species}" Width="*"/>
                <DataGridTextColumn Header="Weight (kg)" Binding="{Binding WeightKg}" Width="160" />
                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove" Padding="8,2"
                                    CommandParameter="{Binding}"
                                    Click="BtnRemoveRow_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Tombol bawah -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Back" Width="100" Click="BtnBack_Click"/>
            <Button Content="Clear" Width="100" Margin="8 0 0 0" Click="BtnClear_Click"/>
            <Button Content="Save" Width="120" Margin="8 0 0 0" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Page>
